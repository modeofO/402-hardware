[package]
name = "lamp-timer"
version = "0.1.0"
edition = "2021"

[dependencies]
esp-idf-svc = { version = "0.50", features = ["critical-section"] }
embedded-graphics = "0.8"
log = "0.4"
anyhow = "1"

[build-dependencies]
embuild = "0.33"
